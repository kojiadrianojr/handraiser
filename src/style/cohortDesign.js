import styled from 'styled-components';

export const BoxMain = styled.div`
    position: relative;
    margin: auto auto;
    max-width: 1024px;
    height: auto;
    padding-bottom: 20px;
    display: flex;
    justify-content: space-evenly;
    flex-wrap: wrap;
`